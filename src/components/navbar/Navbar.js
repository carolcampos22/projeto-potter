import { NavButton, NavbarContainer } from "./styles"

export const Navbar = () => {
    return (
        <NavbarContainer>
            <NavButton>Home</NavButton>
            <NavButton>Spells</NavButton>
        </NavbarContainer>
    )
}